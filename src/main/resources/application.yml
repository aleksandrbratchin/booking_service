server:
  port: 8081
spring:
  application:
    name: booking_service
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
  datasource:
    url: ${db.url}
    username: ${db.username}
    password: ${db.password}
springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger
    tryItOutEnabled: true
resttemplate:
  url:
    customer: http://localhost:8082/customers
    room: http://localhost/rooms
    hotel: http://localhost/hotels